apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - route.yaml
  - networkpolicy.yaml
  - rbac.yaml
  - migration-job.yaml
  - keyvoro-changelogs-cm.yaml

# ExternalSecrets are in ../externalsecrets/ (applied separately or via overlay)
