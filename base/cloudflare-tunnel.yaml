# Cloudflare Tunnel for keyvoro.com / www.keyvoro.com
# Tunnel ID: 32cee07a-2f87-409b-9d4c-c28045a4c0e1
# Routes: keyvoro.com, www.keyvoro.com → keyvoro service (port 3000)
---
apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-token
  namespace: keyvoro
type: Opaque
stringData:
  TUNNEL_TOKEN: "eyJhIjoiNjYyMTcwMGRmYjcxMjMzODcxN2RjZWJiNmEyNDIxMGYiLCJ0IjoiMzJjZWUwN2EtMmY4Ny00MDliLTlkNGMtYzI4MDQ1YTRjMGUxIiwicyI6Ik56aHJXVGx5V2tGaVlVTkVhVTR6YlVVd1JWVnpZMmxOVUZSU1ZsUlJXR1V5UVVaVlJtTlZaRVZrUVQwPSJ9"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflare-tunnel
  namespace: keyvoro
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflare-tunnel
  template:
    metadata:
      labels:
        app: cloudflare-tunnel
    spec:
      serviceAccountName: keyvoro-sa
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - --no-autoupdate
            - --metrics
            - 0.0.0.0:2000
            - run
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflared-token
                  key: TUNNEL_TOKEN
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
          resources:
            requests:
              memory: "64Mi"
              cpu: "25m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /ready
              port: 2000
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /ready
              port: 2000
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 5
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
